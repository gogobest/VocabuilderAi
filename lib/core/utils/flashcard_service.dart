import 'package:visual_vocabularies/features/vocabulary/domain/entities/vocabulary_item.dart';

/// A service to handle flashcard-related operations
class FlashcardService {
  /// Save a flashcard to the database or storage
  Future<void> saveFlashcard({required String word, required String definition}) async {
    // Simulate saving the flashcard (replace with actual implementation)
    print('Saving flashcard: $word - $definition');
  }
}